# Ratatoskr E2E

Central repository for cross-component testing and the overarching test documentation of
the Ratatoskr system.

## Agent skills

### Issue tracker

Issues tracked in this repo's GitHub Issues (`Xexanos/ratatoskr-e2e`) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (CONTEXT.md + docs/adr/ at repo root, created lazily). See `docs/agents/domain.md`.
